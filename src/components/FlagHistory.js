import { View, Text } from "react-native";
import React from "react";

const FlagHistory = () => {
  return (
    <View>
      <Text>FlagHistory</Text>
    </View>
  );
};

export default FlagHistory;
